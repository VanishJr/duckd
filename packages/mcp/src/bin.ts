#!/usr/bin/env node
/**
 * Entry point registered as the `duckd-mcp` binary and referenced from
 * `examples/claude-code/.mcp.json`.
 */

// TODO(bin): parse --transport/--port, then start the server from ./index.js.
console.error('duckd-mcp: not implemented yet, see ROADMAP.md')
process.exit(1)
